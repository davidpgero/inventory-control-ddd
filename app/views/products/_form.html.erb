<%= form_with(model: product, local: true) do |form| %>
  <% if products_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(products_product.errors.count, "error") %> prohibited this products_product from being saved:</h2>

      <ul>
      <% products_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
