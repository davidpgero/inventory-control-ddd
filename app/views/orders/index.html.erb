<p id="notice"><%= notice %></p>

<h1>Orders</h1>

<p><%= link_to 'New Order', new_order_path %></p>

<table>
  <thead>
  <tr>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @orders.each do |order| %>
    <tr>
      <td><%= order.product_name %></td>
      <td><%= order.quantity %></td>
      <td><%= link_to 'Show', order_path(order.uid) %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>

<p><%= link_to 'New Order', new_order_path %></p>